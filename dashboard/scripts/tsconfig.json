{
  "//": "The app's tsconfig leaves JSX alone for Next to compile ('jsx': 'preserve'). This script renders two components outside Next, so it needs a transform — the automatic runtime, which is what Next itself uses, so the components compile here exactly as they do in the app.",
  "extends": "../tsconfig.json",
  "compilerOptions": { "jsx": "react-jsx" }
}
